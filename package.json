{
  "name": "emails-editor",
  "private": true,
  "version": "0.0.1",
  "description": "Component for editing emails",
  "repository": "https://github.com/a-shilova/realtimeboard-test/",
  "license": "MIT",
  "devDependencies": {
    "bower": "^1.7.7",
    "http-server": "^0.9.0"
  },
  "scripts": {
    "update-deps": "npm update",
    "prestart": "npm install",
    "start": "http-server -a localhost -p 8000 -c-1 ./app"
  }
}
